export interface useCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

export interface adCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

