export interface useCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

export interface integrationType {
    image: any,
    width: number,
    height: number,
    title: string,
    content: string,
    buttontext: string
}