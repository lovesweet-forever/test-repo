export interface useCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

export interface startedType {
    image: any,
    width: number,
    height: number,
    title: string,
    buttontext: string
}