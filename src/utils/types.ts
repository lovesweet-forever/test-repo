export interface useCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

export interface startedType {
export interface adCardType {
    image: any,
    width:number,
    height:number,
    title: string,
    description: string
}

export interface integrationType {
    image: any,
    width: number,
    height: number,
    title: string,
    buttontext: string
}
export interface typeCommerce{
    tabItem:String,
    title:String,
    smmary:String,
    articleItems:String[],
    buttonTitle:String,
    picture:any
}
